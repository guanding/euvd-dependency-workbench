name: EUVD matcher CodeQL configuration

paths:
  - app
  - scripts
  - release
  - tests

paths-ignore:
  - data
  - backups
  - outputs
  - runtime
  - self-test

queries:
  - uses: security-extended
  - uses: security-and-quality
